---
name: ppt-simplifier
description: 把老师的教学课件(尤其是电子信息类专业课:信号与系统、模电、数电、通信原理、电磁场、概率论等)改写成自己看得懂的「学霸讲解版」PPT。当用户给出一份 pptx 课件并说"看不懂""听不懂""帮我讲简单点""重做一份通俗易懂的""做成能自学考的",或要求保留老师 PPT 里的例题、公式、波形图、电路图时使用。输入老师 PPT,输出通俗版 PPT:每页一个知识点、先白话解释再讲例题,例题与公式无损保留。
---

# PPT 通俗化改写(电子信息专业课)

把老师的 PPT 改写成「零基础自学者也能看懂」的版本。核心承诺:**例题、公式、图 100% 无损保留**(做法是复用原页渲染图,而不是转写/重画——转写公式极易出错,这样最稳)。

## 输入与应试重点

主输入是老师的 PPT。**如果用户同时给了老师布置的作业题/练习(电子版、拍照、截图、PPT 末尾的课后练习都行),把作业题当作「重点信号」:**

- PPT 决定**学什么**;作业题决定**重点学什么、学到多深**。作业题只用于给内容打重点标签和加密讲解,绝不用于删减 PPT 里的内容(PPT 讲了但作业没考的知识点仍可能出填空题/选择题,要做但轻处理)。
- 重点标签用 `exam` 字段,只取三个值:`必考`(作业题直接考到的知识点/题型)、`掌握`(作业题间接涉及、课上反复强调的)、`了解`(PPT 里有但作业没涉及)。没把握就不标,不要乱标。
- 作业题里出现、但 PPT 讲得含糊或没细讲的题型,在通俗版末尾补一页「老师说会考但 PPT 没细讲」,按 `concept` 页处理,`exam` 标 `必考`,用作业题的答案讲思路。
- 例题页若恰好是作业真题,解析要按「这题是真题」来写:给套路、给易错点、给可能变体。
- **老师 PPT 里的例题本身就是重点,必须重视**:每道例题都独立保留为一个 `example` 页(原图+解析),不允许因为"版面塞不下"就把两道例题并页或砍掉解析;解析至少讲三点——这题在考什么、第一步为什么这么做、最容易错在哪。给例题定 `exam` 级别时默认不低于「掌握」,凡是对应作业题或属于高频题型的标「必考」。
- **用户给的作业题必须逐题分析并显式联动,绝不能只打标签了事**:
  1. 每道题先提取考点(如增益计算、输出电阻、源电压增益、信号表示法),列一张「作业题 → 考点」清单
  2. 把每个考点映射到课件的知识块/页面,写出「作业题 → 关联页」清单
  3. **即使作业题与课件不是同一章**(如课件第 2 章、作业题第 1 章):只要考点在课件中有呼应(如"电压放大电路模型"↔"运放三指标/输入输出电阻"),就必须在相关页的解析里写「呼应作业题 x.x.x」,并把该考点讲透(公式、概念、常见题型思路),在封面/路线图页也要交代前情关联;不得以"章节不同"为由跳过
  4. 动笔写 outline 之前先完成以上映射,映射结果要体现为页面的 `exam` 标签与解析文字

## 不可违背的规则

1. **例题页永远复用原页截图**:`assets/pages/slide_XXX.png` 整页放进新 PPT 的例题页,不要转写题目、不要自己重画公式。可以在旁边配白话解析,但题目本身必须是原图。
2. **公式优先用原页截图**:讲解页牵涉公式时,优先 `source` 字段贴原页/公式截图;仅当找不到合适截图时才用 `latex` 文本(matplotlib mathtext 渲染),且必须对照原页图片逐字核对后再转录。
3. **每个专业名词第一次出现,必须在句子里就地用括号给"人话解释"**,同时把本页出现的所有名词汇总进该页的 `terms` 字段(渲染成页面底部的「📚 名词小抄」框,一条一个:「名词:大白话解释」)。读者是自学、零基础——看不懂术语就像读天书。宁可多解释,不可默认读者懂;不要把术语堆进句子不解释,不要用别的术语来解释术语。
   - 反例:"根据虚短虚断列 KCL 得 Av=−R2/R1"(读者不知道虚短/虚断/KCL/Av 是什么)
   - 正例:"两个输入端电位被拉到相等(虚短)→ 反相端电压≈0;输入端几乎不进电流(虚断)→ 电流 i1 只能全走电阻 R2;对节点列电流守恒式,解出放大倍数 Av=−R2/R1"
4. 新 PPT 页数通常**多于**原 PPT(拆细),绝不因为"版面不够"而砍内容。

## 步骤 1:提取

```bash
python scripts/extract.py --input "老师的课件.pptx" --work ./work
```

产物:
- `work/slides_text.json` — 每页文本(可能不含公式!)
- `work/assets/pages/slide_001.png` — 每页 200 DPI 高清渲染图,**公式/波形/电路图都在图里**
- `work/assets/images/` — PPT 内嵌图片

**渲染链路(自动降级)**:① PowerPoint COM → ② LibreOffice(`soffice --convert-to pdf` + PyMuPDF)。第一条失败会自动走第二条。若两条都失败,用备选路径:文本 + 内嵌图片,公式页请用户确认或跳过,绝不可凭想象补公式。

**含公式 OLE 的课件(MathType / Equation 3.0 / Word.Picture.8)**:这类公式编辑器对象常见,PowerPoint COM 常常连文件都打不开。判断方法:`slides_text.json` 正常但渲染失败,或原文件里 `oleObj` 极多。处理:
- 首选直接走 LibreOffice(内置 MTEF 公式渲染,已验证可正确渲染公式)
- 若 LibreOffice 效果不好,可先跑 `scripts/rasterize_ole.py --input x.pptx --output 栅格化.pptx --work ./work`(把可从 OLE 中提取的 EMF/WMF 图形转成 PNG 插回原位;纯 MTEF 无 EMF 时自动跳过,保留原样)再渲染

依赖:PyMuPDF;渲染二选一(PowerPoint 或 LibreOffice)。

## 步骤 2:读懂课件

1. 先读 `slides_text.json`,知道每页讲什么、哪些页是例题/公式页。
2. 对公式页、例题页、讲得抽象的概念页,用 Read 看对应 `assets/pages/*.png` 的**原图**——这是读懂公式的唯一可靠途径。看不懂就多看几遍,看懂了再往下走。
3. 梳理知识主线:前几页讲了什么概念?大纲是按什么顺序展开的?后面例题用的公式从哪来?列出 3~8 个知识块即可,不要写长文档。

## 步骤 3:写 outline.json

按知识块规划新 PPT,在 `work/outline.json` 里写每页的"通俗表述"而不是照搬原文。**如果用户给了作业题:先把作业题的考点映射到知识块,定好 `exam` 级别,再动笔写每页。** 逐页类型:

- `cover` — 封面:`title` 课程名(通俗版),`subtitle` 如"自学者友好 · 基于老师课件改写"
- `concept` — 概念页:`title` 白话标题(如"什么叫傅里叶变换——把信号拆成音符"),`one_liner` 一句话版本,`bullets` 3~5 条要点(每条都是大白话+术语括号注人话),`analogy` 生活类比
- `formula` — 公式页:`title`,无 `source` 时用 `latex`(JSON 中反斜杠写 `\\`,如 `"x(t)=\\sum_{n} a_n e^{j n \\omega_0 t}"`),`note` 逐符号人话解释
- `example` — 例题页:**必须** `source: "assets/pages/slide_XXX.png"`(原页截图,等比缩放自动适配),`source_label: "原课件第X页"`,`analysis` 此题在考什么(3~4 句白话),`steps` 步骤列表
- `summary` — 小结页:`title`(如"第2章小结 · 你记住了哪些?"),`bullets` 3~5 句主线程回顾

以上每一页都可以加 `exam` 字段(限 `必考`/`掌握`/`了解`),build 会在标题栏右侧渲染出彩色标签;不加则无标签。

例题页识别:文本含「例」「例题」「习题」「试求」「证明」,或页面上是题目型内容(条件+问题)的页,一律按 `example` 处理——保留永远比删除安全。例题页若贴了原图,新页里可以不带任何转写的题面文字。

示例结构(节选):

```json
{
  "title": "信号与系统(通俗版)",
  "subtitle": "自学者友好 · 基于老师课件改写",
  "output": "信号与系统(通俗版).pptx",
  "slides": [
    {"type": "cover"},
    {"type": "concept",
     "title": "什么是系统——黑箱与大白箱",
     "one_liner": "系统就是"输入进、输出出的黑箱"",
     "exam": "掌握",
     "bullets": [
       "系统 = 一个黑箱:扔进输入 x(t),吐出输出 y(t)",
       "本书研究的对象:线性、时不变系统(LTI 系统,意思是:按比例放大输入,输出也按比例放大)",
       "为什么学:真实世界的电路、通信、机械都能近似成 LTI 系统"
     ],
     "analogy": "就像奶茶店:下单是小票(输入),出杯是成品(输出),配方不变就是"时不变""},
    {"type": "example",
     "title": "例题(原第9页)",
     "exam": "必考",
     "source": "assets/pages/slide_009.png",
     "source_label": "原课件第9页",
     "analysis": "这题考你熟练用欧拉公式把正弦信号拆成复指数累加。关键一步是把 sin 的展开式带上相位,直接代入公式即可。",
     "steps": ["用欧拉公式展开 sin(ωt+φ)", "拆成两路复指数并合并系数", "套结论:系数即频谱"]},
    {"type": "summary",
     "title": "第1章小结",
     "bullets": ["…","…"]}
  ]
}
```

## 步骤 4:生成

```bash
python scripts/build.py --outline ./work/outline.json --work ./work
```

生成 `work/<output>` 并自动渲染成 `work/check/slide_*.png`。

## 步骤 5:逐页检查(必须)

build 结束时已自动做两件事:
1. 渲染出 `work/check/slide_*.png`
2. 跑几何检查(文本框溢出/越界/重叠),发现问题会让 build 失败并列出位置——按提示修改 `outline.json`(通常缩短文字)后重新 build。

几何检查通过后,再人工过一遍渲染图:
- 用 Read 看 `work/check/*.png`,确认例题原图完整清晰、公式完整、文字没有溢出容器、类比条/解析条没被裁切
- 没有漏页:每类内容都在(概念页、公式页、每个例题、小结)

发现布局问题(溢出/重叠)优先改 `outline.json`(缩减文字、精简步骤),不要改动 build.py 的布局参数来迁就内容。

## 交付与清理(每份 PPT 完成后必做)

1. 成品 PPT 复制/输出到用户要求的目录(没有要求就放在工作目录,并告知路径)。
2. **删除只与这份 PPT 相关的一次性文件**,包括:工作目录(`work/` 提取与检查产物)、临时诊断脚本、源文件副本、栅格化/修复测试文件、样例数据。**必须保留**(属于通用工具,删了下次没法干):
   - skill 本体:SKILL.md + `scripts/`(extract.py、build.py、render.py、check_layout.py、rasterize_ole.py)
   - Python 库依赖(python-pptx、PyMuPDF、Pillow、matplotlib、olefile、RapidOCR 等)
   - 渲染器:PowerPoint 或 LibreOffice
   - 本次成品 PPT 本身
3. 清理前先列出清单,对照"通用 vs 一次性"再删。
