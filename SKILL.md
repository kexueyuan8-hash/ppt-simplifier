---
name: ppt-simplifier
description: 把老师的教学课件(尤其是电子信息类专业课:信号与系统、模电、数电、通信原理、电磁场、概率论等)改写成适合零基础自学与应试复习的中文通俗版 PPT。当用户提供 pptx 课件并说"看不懂""帮我讲简单点""做成能自学的版本",或要求保留例题、公式、电路图、波形图,或另外提供作业题/照片要求联动重点时使用。输入老师 PPT(可附作业题),输出通俗版 PPTX 与可编辑大纲。
---

# 教学 PPT 通俗化

目标:把老师课件改成学生能独立看懂、能据此复习考试的讲义版 PPT。知识范围以老师课件为边界;作业题只用于判断重点和讲解深度,不用于删减课件已覆盖的内容。输出中文。

开始处理前必须阅读:

- [用户要求](references/用户要求.md):不可违背的内容规则与验收要求。
- [大纲格式](references/大纲格式.md):`outline.json` 的字段、页面类型和示例。

## 工作流程

1. 确认三样东西:老师原课件、作业题(或截图,可无)、输出目录。用户未指定输出目录时,在工作区建立 `做完的PPT放这/`。
2. 提取原课件:

   ```bash
   python scripts/extract.py --input "老师课件.pptx" --work ./work
   ```

   得到 `work/slides_text.json`(每页文本)、`work/assets/pages/slide_XXX.png`(每页 200 DPI 渲染图,公式都在图里)、`work/assets/images/`(内嵌图片)。
3. 阅读 `slides_text.json` 并检查 `assets/pages/`。**字符数少于 12 的页面必须 OCR 或直接看图,禁止按"空页"跳过**——整页图片的例题/公式页文本提取是空的,跳过就会漏例题。OCR 可用 `rapidocr_onnxruntime` 对渲染图直接识别。
4. **写大纲前建立原课件逐页覆盖表**(`page_coverage`)。每页只能处于以下状态之一:已并入概念页、作为例题独立保留、作为公式或图示来源保留、作为过渡页并写明跳过理由。一页都不能凭空消失。
5. **若有作业题**:先完成"题号 → 考点 → 原课件页 → 新 PPT 页"的映射(`homework_map`),再写 `outline.json`。即使作业与课件章节编号不同,只要知识点相关就必须关联;相关页面正文写"呼应作业题 X.X.X"。
6. 按 [大纲格式](references/大纲格式.md) 写通俗版内容。专业名词首次出现时在原句括号内解释,并同步放入本页 `terms`。例题解析必须回答三问:考什么、第一步为什么这样做、最容易错在哪里;例题页整页复用原页高清图,不转写、不重画、不裁切。
7. 生成并检查:

   ```bash
   python scripts/build.py --outline ./work/outline.json --work ./work
   python scripts/check_layout.py --input "./work/成品.pptx"
   ```

   build 末尾已自动跑布局检查;报溢出/重叠时**改大纲(通常缩短文字)重跑**,不要改脚本迁就内容。
8. 渲染全部成品页并**逐页查看(不是只看缩略图)**:

   ```bash
   python scripts/render.py --input "./work/成品.pptx" --out ./work/check --dpi 200
   ```

9. 修复漏页、例题不完整、公式或图不清楚、文字溢出、遮挡、单位错误,重跑第 7~9 步直到全部通过。
10. 交付:成品 PPTX 与可编辑 `outline.json` 复制到交付目录(建议在成品旁留「大纲(可编辑)/」文件夹,便于日后改版不必重新提取)。确认交付成功后,清理本次工作目录、检查图与临时文件;**保留**原课件、技能包、通用依赖、渲染器、成品和可编辑大纲。

## 工具规则(含实战坑位)

- **渲染链路**:PowerPoint COM 优先,失败自动降级 LibreOffice(`render.py` 内已实现,无需手工切换)。
- **含 OLE 公式的课件**(MathType / Equation / Word.Picture):PowerPoint COM 常直接打不开(报 "could not open the file"),重打包、去 OLE 都不能解决——不要纠结,让 `render.py` 走 LibreOffice,它能正确渲染这类公式(已实测)。
- `rasterize_ole.py` 仅作备选:若 OLE 内是 MTEF(无 EMF/WMF 可提取),0 命中属预期行为,保持原状交给 LibreOffice 即可。
- **公式、电路图、波形图、例题题面一律以原页渲染图为唯一依据**;看不清或渲染失败要如实报告,绝不凭印象重建。
- 数值与单位必须核对:`f=1/T`、`ω=2πf`(给周期先统一时间单位再取倒数;题目已给 `rad/s` 时不再乘 `2π`);正弦无直流偏移时峰峰值÷2=峰值、有效值=峰值÷√2;dB 电压/电流用 `20lg`、功率用 `10lg`;`Hz/kHz`、`s/ms`、`Ω/kΩ/MΩ`、`A/mA/μA` 先统一再算;引用数字回原图核对,OCR 结果(题号/下标易错)标注"以原件为准"。
- Windows 细节:PowerShell 从 Python 子进程调用时输出 GBK;PowerPoint 导出的 PNG 文件名是中文"幻灯片N.PNG"(脚本已处理排序与重命名);目标文件被 PowerPoint 占用时覆盖会失败——改用副本名交付并告知用户。
- 缺少 Python 依赖时按 `requirements.txt` 安装。

## 完成标准

只有同时满足以下条件,才能向用户报告"已完成":

- 原课件每一页都有覆盖记录(`page_coverage` 完整)。
- 每道例题独立占一页,并完整保留原页高清图(不转写、不裁切)。
- 每道例题都有"考什么、第一步为什么、最容易错在哪里"的解释。
- 所有低文本页已通过 OCR 或看图确认。
- 作业题已逐题映射(`homework_map`)并在对应讲解页显式联动。
- 数值、单位、公式、电路连接及波形均回看原图核对。
- 自动布局检查通过,且所有成品页已逐页目视检查。
- 成品 PPTX 与可编辑大纲已放入交付目录,一次性文件已清理。
